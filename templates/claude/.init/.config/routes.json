{
  "gateUnmatched": true,
  "rules": [
    { "channel": "*", "isDM": true, "thread": "${channel}" },
    { "channel": "#*", "thread": "${channel}" }
  ],
  "sessions": {
    "#*": { "batch": { "debounce": 20, "maxWait": 120, "triggers": ["@{{name}}"] } }
  },
  "default": "main"
}
