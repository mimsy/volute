{
  "gateUnmatched": true,
  "rules": [
    { "channel": "*", "isDM": true, "session": "${channel}" },
    { "channel": "#*", "session": "${channel}" }
  ],
  "sessions": {
    "#*": { "batch": { "debounce": 20, "maxWait": 120, "triggers": ["@{{name}}"] } }
  },
  "default": "main"
}
