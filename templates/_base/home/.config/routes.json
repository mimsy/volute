{
  "rules": [{ "channel": "mail:*", "thread": "mail" }, { "channel": "#*", "thread": "${channel}" }],
  "threads": {
    "#*": { "batch": { "debounce": 20, "maxWait": 120, "triggers": ["@{{name}}"] } }
  }
}
