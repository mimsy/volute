services:
  volute:
    build: .
    ports:
      - "1618:1618"
    environment:
      - TZ=${TZ:-UTC}
    volumes:
      - volute-data:/data
      - volute-minds:/minds
      - ~/.claude:/data/.claude
    restart: unless-stopped

volumes:
  volute-data:
  volute-minds:
