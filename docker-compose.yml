services:
  volute:
    build: .
    ports:
      - "4200:4200"
    volumes:
      - volute-data:/data
      - volute-agents:/agents
    restart: unless-stopped

volumes:
  volute-data:
  volute-agents:
