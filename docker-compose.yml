services:
  volute:
    build: .
    ports:
      - "4200:4200"
    environment:
      - TZ=${TZ:-UTC}
    volumes:
      - volute-data:/data
      - volute-minds:/minds
      - ~/.claude:/data/.claude
    restart: unless-stopped

volumes:
  volute-data:
  volute-minds:
